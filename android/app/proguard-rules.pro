# WebView shell app — tidak ada obfuscation khusus yang dibutuhkan saat ini.
# minifyEnabled dimatikan (lihat build.gradle) supaya console.log dari WebView
# tetap gampang ditelusuri lewat chrome://inspect saat debugging.
